# Copy to .env and fill in your keys. Never commit .env.

GEMINI_API_KEY=
GROQ_API_KEY=

# Comma-separated CORS origins for production (HF Spaces → Vercel).
# Default allows local development on common Vite ports.
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175
